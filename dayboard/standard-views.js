import {esc,dateKey,timeKey,at,addDays,weekStart,monthDays,dayOfWeek,duration,deadlineText,leaves,priority,growth,overlaps} from './core.js';
export const NAV=[['today','오늘','sun'],['week','이번 주','week'],['month','월간','calendar'],['board','업무 보드','board'],['projects','프로젝트','folder'],['settings','연결·설정','settings']];
const paths={sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2m0 16v2M2 12h2m16 0h2M5 5l1.5 1.5m11 11L19 19M5 19l1.5-1.5m11-11L19 5"/>',week:'<rect x="3" y="4" width="18" height="17" rx="3"/><path d="M3 9h18M8 2v4m8-4v4M9 9v12m6-12v12"/>',calendar:'<rect x="3" y="4" width="18" height="17" rx="3"/><path d="M3 9h18M8 2v4m8-4v4M7 13h2m3 0h2m3 0h1M7 17h2m3 0h2"/>',board:'<rect x="3" y="4" width="18" height="16" rx="3"/><path d="M9 4v16m6-16v16M6 8v4m6-4v7m6-7v3"/>',folder:'<path d="M3 7a2 2 0 012-2h5l2 3h7a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V7z"/>',settings:'<path d="M4 7h16M4 17h16"/><circle cx="9" cy="7" r="3" fill="white"/><circle cx="16" cy="17" r="3" fill="white"/>',plus:'<path d="M12 5v14M5 12h14"/>',check:'<path d="M5 12l4 4L19 6"/>',chevron:'<path d="M9 5l7 7-7 7"/>',back:'<path d="M15 5l-7 7 7 7"/>',close:'<path d="M6 6l12 12M6 18L18 6"/>',search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="M16 16l5 5"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',spark:'<path d="M12 3l2.5 6.5L21 12l-6.5 2.5L12 21l-2.5-6.5L3 12l6.5-2.5L12 3z"/>',refresh:'<path d="M20 10a8 8 0 00-14-5L3 8m0-5v5h5M4 14a8 8 0 0014 5l3-3m0 5v-5h-5"/>',lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 018 0v3m-4 4v3"/>',trophy:'<path d="M8 3h8v8a4 4 0 01-8 0V3zm0 2H4v3a4 4 0 004 4m8-7h4v3a4 4 0 01-4 4m-4 3v5m-4 1h8"/>',more:'<circle cx="5" cy="12" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/>',arrow:'<path d="M5 12h14m-5-5l5 5-5 5"/>',download:'<path d="M12 3v12m-5-5l5 5 5-5M4 16v5h16v-5"/>',upload:'<path d="M12 16V4m-5 5l5-5 5 5M4 16v5h16v-5"/>',link:'<path d="M10 13a5 5 0 007 0l3-3a5 5 0 00-7-7l-2 2M14 11a5 5 0 00-7 0l-3 3a5 5 0 007 7l2-2"/>',logout:'<path d="M10 4H5v16h5m-2-8h13m-4-4l4 4-4 4"/>'};
export const icon=name=>`<svg class="icon" aria-hidden="true" viewBox="0 0 24 24">${paths[name]||paths.calendar}</svg>`;
export const btn=(action,text,{id='',cls='',ico='',title=''}={})=>`<button class="button ${cls}" data-action="${action}" ${id?`data-id="${esc(id)}"`:''} ${title?`title="${esc(title)}"`:''}>${ico?icon(ico):''}${text}</button>`;
export const ibtn=(action,ico,label,id='')=>`<button class="iconbutton" data-action="${action}" ${id?`data-id="${esc(id)}"`:''} title="${esc(label)}" aria-label="${esc(label)}">${icon(ico)}</button>`;
export const empty=(title,sub='',action='',text='')=>`<div class="empty"><strong>${title}</strong>${sub}${action?`<div>${btn(action,text)}</div>`:''}</div>`;
export const progress=n=>`<div class="progress" role="progressbar" aria-label="진행률" aria-valuenow="${Math.max(0,Math.min(100,Number(n)||0))}" aria-valuemin="0" aria-valuemax="100"><span style="width:${Math.max(0,Math.min(100,Number(n)||0))}%"></span></div>`;
export const check=i=>`<button class="check ${i.status==='done'?'done':''}" data-action="complete" data-id="${esc(i.id)}" aria-label="${esc(i.title)} ${i.status==='done'?'완료 취소':'완료'}">${i.status==='done'?icon('check'):''}</button>`;
export const dayBlocks=(s,d)=>s.blocks.filter(b=>+new Date(b.end)>+at(d)&&+new Date(b.start)<+at(addDays(d,1))).sort((a,b)=>+new Date(a.start)-+new Date(b.start));
export function taskRow(i,items){const parent=items.find(p=>p.id===i.parentId);return `<div class="task-row ${i.status==='done'?'done':''}" draggable="true" data-drag-type="item" data-id="${esc(i.id)}">${check(i)}<div class="grow"><button class="title-button" data-action="edit-item" data-id="${esc(i.id)}">${esc(i.title)}</button><div class="meta">${parent?`<span>${esc(parent.title)}</span>`:''}<span>${duration(i.estimatedMinutes)}</span>${i.deadline?`<span class="${i.deadline<=dateKey()&&i.status!=='done'?'tag red':''}">${deadlineText(i.deadline)}</span>`:''}${i.recurrence!=='none'?'<span>반복</span>':''}</div></div><div class="actions">${i.kind!=='project'?ibtn('schedule-item','clock','시간 배치',i.id):''}${ibtn('edit-item','more','상세 수정',i.id)}</div></div>`;}
function nav(view,mobile=false){return NAV.filter(n=>!mobile||n[0]!=='projects').map(([id,label,ico])=>`<button data-action="nav" data-id="${id}" class="${view===id?'active':''}" aria-current="${view===id?'page':'false'}">${icon(ico)}<span>${label}</span></button>`).join('');}
export function connectPage(){return `<main class="connect-page"><section><div class="brand"><span class="brandmark">D</span>Dayboard</div><h1>복잡한 일정은 덜고,<br>오늘은 선명하게.</h1><p>오늘의 할 일부터 이번 주의 흐름까지.<br>하나의 보드에서 계획하고, 완료하세요.</p><form id="connect-form"><label for="connect-key">내 보드 연결키</label><input id="connect-key" name="key" type="password" placeholder="전달받은 연결키를 붙여넣으세요" autocomplete="off" required minlength="32" maxlength="256"><p class="hint">같은 연결키를 PC와 모바일에서 사용하면 일정이 동기화됩니다. 연결키는 비밀번호처럼 보관하세요.</p><div class="actions"><button class="button primary" type="submit">내 보드 열기 ${icon('arrow')}</button>${btn('demo','샘플 둘러보기',{cls:'ghost'})}</div><div id="connect-error" class="error-text" role="alert"></div></form><p class="hint space-top">유료 AI API 없이 · 개인 클라우드 저장 · 승인 후 일정 반영</p></section><aside class="connect-preview" aria-label="샘플 화면"><div class="row" style="justify-content:space-between"><h2>오늘의 흐름</h2><span class="tag blue">PREVIEW</span></div><div class="preview-row"><span class="preview-time">09:00</span><span class="check"></span><span>오늘 가장 중요한 일</span></div><div class="preview-row"><span class="preview-time">11:00</span><span class="check"></span><span>진행 중인 프로젝트</span></div><div class="preview-row"><span class="preview-time">15:00</span><span class="check"></span><span>집중해서 마무리하기</span></div><span class="preview-label">실제 일정이 아닌 화면 구성 예시입니다.</span></aside></main>`;}
function heading(ctx){const {date,view,state:s}=ctx;const [year,month,day]=date.split('-').map(Number);let title=`${month}월 ${day}일 ${'일월화수목금토'[dayOfWeek(date)]}요일`;let subtitle='해야 할 일과 하루의 흐름을 한눈에 확인하세요.';if(view==='week'){const start=weekStart(date),end=addDays(start,6);title=`${+start.slice(5,7)}월 ${+start.slice(8)}일 — ${+end.slice(5,7)}월 ${+end.slice(8)}일`;subtitle='시간이 정해진 일정과 마감 업무를 함께 봅니다.';}if(view==='month'){title=`${year}년 ${month}월`;subtitle='중요한 마감과 약속, 한 달의 흐름을 확인하세요.';}if(view==='board'){title='업무 보드';subtitle='카드를 옮기거나 상세 화면에서 상태를 바꾸세요.';}if(view==='projects'){title='프로젝트';subtitle='큰 목표를 작은 실행으로 연결하세요.';}if(view==='settings'){title='연결·설정';subtitle='일하는 시간, 동기화, 데이터 관리를 한곳에서.';}return `<div class="page-heading"><div><div class="eyeline">${view==='today'?(date===dateKey()?'TODAY':'DAY VIEW'):view==='week'?'THIS WEEK':view==='month'?'MONTHLY':view==='board'?'WORKFLOW':view==='projects'?'PROJECTS':'PREFERENCES'}</div><h1>${title}</h1><p class="muted">${subtitle}</p></div>${['today','week','month'].includes(view)?`<div class="date-controls">${ibtn('prev','back','이전')}${btn('jump-today','오늘')}${ibtn('next','chevron','다음')}</div>`:view==='projects'?btn('add-project','프로젝트',{ico:'plus'}):''}</div>`;}
function miniCalendar(ctx){const {date,state}=ctx;return `<div class="rail-section calendar-rail"><div class="section-head"><h2>${+date.slice(5,7)}월</h2>${btn('nav','월간 보기',{id:'month',cls:'ghost'})}</div><div class="mini-calendar">${'월화수목금토일'.split('').map(d=>`<span class="weeklabel">${d}</span>`).join('')}${monthDays(date).map(d=>`<button class="mini-day ${d.slice(0,7)!==date.slice(0,7)?'outside':''} ${d===dateKey()?'today':''} ${d===date?'selected':''} ${dayBlocks(state,d).length?'has-events':''}" data-action="select-day" data-id="${d}" aria-label="${d}">${+d.slice(8)}</button>`).join('')}</div></div>`;}
function rail(ctx){const projects=ctx.state.items.filter(i=>i.kind==='project'&&i.status!=='done');const dues=leaves(ctx.state.items).filter(i=>i.deadline&&i.status!=='done').sort((a,b)=>a.deadline.localeCompare(b.deadline)).slice(0,4);return `<aside class="rail"><section class="rail-section"><div class="section-head"><h2>진행 중인 프로젝트</h2>${ibtn('add-project','plus','프로젝트 추가')}</div>${projects.length?projects.slice(0,5).map(p=>`<button class="project-mini" data-action="edit-item" data-id="${p.id}"><div class="row"><span>${esc(p.title)}</span><span class="muted tiny">${p.progress}%</span></div>${progress(p.progress)}</button>`).join(''):'<p class="empty-plain">큰 목표가 있다면<br>프로젝트로 묶어 보세요.</p>'}</section>${miniCalendar(ctx)}<section class="rail-section rail-due"><div class="section-head"><h2>다가오는 마감</h2>${icon('clock')}</div>${dues.length?dues.map(i=>`<div class="due-item"><span class="due-date">${+i.deadline.slice(5,7)}/${+i.deadline.slice(8)}</span><div><button data-action="edit-item" data-id="${i.id}">${esc(i.title)}</button><div class="meta">${deadlineText(i.deadline)}</div></div></div>`).join(''):'<p class="empty-plain">등록된 마감이 없습니다.</p>'}</section></aside>`;}
function timeline(ctx){const {state,date}=ctx;const day=dayBlocks(state,date);const timed=day.filter(b=>!b.allDay);let startHour=8,endHour=20;for(const b of timed){startHour=Math.min(startHour,dateKey(b.start)<date?0:+timeKey(b.start).split(':')[0]);endHour=Math.max(endHour,dateKey(b.end)>date?24:Math.ceil((+new Date(b.end)-+at(date))/3600000));}startHour=Math.max(0,startHour);endHour=Math.min(24,endHour);let groups=[],group=[],lastEnd=0;for(const b of timed){const start=+new Date(b.start);if(group.length&&start>=lastEnd){groups.push(group);group=[];lastEnd=0;}group.push(b);lastEnd=Math.max(lastEnd,+new Date(b.end));}if(group.length)groups.push(group);const positioned=[];for(const group of groups){const lanes=[];let max=0;for(const b of group){let lane=lanes.findIndex(e=>e<=+new Date(b.start));if(lane<0)lane=lanes.length;lanes[lane]=+new Date(b.end);max=Math.max(max,lanes.length);positioned.push({b,lane,group});}group.cols=max;}const px=60;const origin=+at(date,`${String(startHour).padStart(2,'0')}:00`);const height=(endHour-startHour)*px;const now=(Date.now()-origin)/60000;return `<section class="panel"><div class="panel-head"><div><h2>오늘의 타임라인</h2><div class="meta">${day.length}개 일정 · ${duration(timed.reduce((n,b)=>n+Math.max(0,Math.round((Math.min(+at(addDays(date,1)),+new Date(b.end))-Math.max(+at(date),+new Date(b.start)))/60000)),0))}</div></div>${btn('new-block','일정 추가',{ico:'plus'})}</div>${day.some(b=>b.allDay)?`<div class="all-day">${day.filter(b=>b.allDay).map(b=>`<button data-action="edit-block" data-id="${b.id}">종일 · ${esc(b.title)}</button>`).join('')}</div>`:''}<div class="timeline-wrapper"><div class="timeline" style="height:${height}px;min-height:${height}px">${Array.from({length:endHour-startHour},(_,i)=>`<div class="hour" data-drop-hour="${date}|${startHour+i}" title="클릭해서 시간대에 추가"><div class="hour-label">${String(startHour+i).padStart(2,'0')}:00</div><div class="hour-line"></div></div>`).join('')}<div class="time-events">${positioned.map(({b,lane,group})=>{const top=Math.max(0,(+new Date(b.start)-origin)/60000);const bottom=Math.min(height,(+new Date(b.end)-origin)/60000);const done=state.items.find(i=>i.id===b.taskId)?.status==='done';return `<button class="time-block ${b.source==='google'?'external':''} ${done?'finished':''}" style="top:${top}px;height:${Math.max(24,bottom-top)}px;left:${lane/group.cols*100}%;width:calc(${100/group.cols}% - 4px);z-index:${lane+1}" draggable="true" data-drag-type="block" data-action="edit-block" data-id="${b.id}" title="${esc(b.title)} · ${timeKey(b.start)}–${timeKey(b.end)}"><span class="block-title">${b.locked?'⌑ ':''}${esc(b.title)}</span>${bottom-top>=40?`<span class="block-time">${timeKey(b.start)}–${timeKey(b.end)}${b.source==='google'?' · Google':''}</span>`:''}</button>`;}).join('')}</div>${date===dateKey()&&now>=0&&now<=height?`<div class="now-line" style="top:${now}px"></div>`:''}</div></div></section>`;}
function todayView(ctx){const {state:s,date}=ctx;const pending=leaves(s.items).filter(i=>i.status!=='done');const focus=pending.filter(i=>!i.deadline||i.deadline<=addDays(date,7)).sort((a,b)=>priority(b)-priority(a)).slice(0,3);const inbox=pending.filter(i=>!s.blocks.some(b=>b.taskId===i.id&&+new Date(b.end)>Date.now()));return `<div class="layout"><main class="main-column"><section class="focus"><div class="section-head"><h2>먼저 할 일</h2><span class="tiny muted" title="마감·중요도·긴급도를 기준으로 정렬">우선순위 기준</span></div><div class="focus-list">${focus.length?focus.map((i,n)=>`<div class="focus-item"><span class="focus-number">0${n+1}</span>${check(i)}<div class="grow"><button class="title-button" data-action="edit-item" data-id="${i.id}">${esc(i.title)}</button><div class="meta"><span>${duration(i.estimatedMinutes)}</span><span>${deadlineText(i.deadline)}</span></div></div>${ibtn('schedule-item','clock','시간 배치',i.id)}</div>`).join(''):empty('지금은 비어 있는 보드입니다.','첫 번째 할 일을 추가해 하루를 시작하세요.','add-item','할 일 추가')}</div></section><form id="quick-form" class="quick-input"><div class="input-row"><input name="title" placeholder="할 일 추가 · 제목만 적어도 충분해요" maxlength="200" required aria-label="빠른 할 일 추가"><button class="iconbutton" type="submit" aria-label="할 일 저장">${icon('plus')}</button></div></form>${timeline(ctx)}<section class="panel space-top"><div class="panel-head"><h2>아직 시간 미정 <span class="muted small">${inbox.length}</span></h2>${btn('plan','빈 시간에 배치',{ico:'spark'})}</div><div class="task-list">${inbox.slice(0,12).map(i=>taskRow(i,s.items)).join('')||'<div class="empty-plain">시간을 배치할 업무가 없습니다.</div>'}${inbox.length>12?btn('nav',`전체 ${inbox.length}개 보기`,{id:'board',cls:'ghost'}):''}</div></section></main>${rail(ctx)}</div>`;}
function weekView(ctx){const dates=Array.from({length:7},(_,i)=>addDays(weekStart(ctx.date),i));return `<div class="filters">${btn('plan','빈 시간에 배치',{ico:'spark'})}${btn('replan','밀린 일정 재배치',{ico:'refresh'})}${btn('chat-week','ChatGPT로 계획',{cls:'ghost',ico:'link'})}</div><div class="week-board">${dates.map(d=>{const blocks=dayBlocks(ctx.state,d);const due=leaves(ctx.state.items).filter(i=>i.deadline===d&&i.status!=='done');return `<section class="week-day ${d===dateKey()?'today':''}" data-drop-day="${d}"><button class="week-day-header" data-action="select-day" data-id="${d}">${'일월화수목금토'[dayOfWeek(d)]}<strong>${+d.slice(8)}</strong></button>${blocks.map(b=>`<button class="week-event ${b.source==='google'?'external':''}" draggable="true" data-drag-type="block" data-action="edit-block" data-id="${b.id}"><span>${b.allDay?'종일':timeKey(b.start)+'–'+timeKey(b.end)}</span>${esc(b.title)}</button>`).join('')}${due.map(i=>`<button class="week-todo" data-action="edit-item" data-id="${i.id}">마감 · ${esc(i.title)}</button>`).join('')}${!blocks.length&&!due.length?'<div class="empty-plain">여유 있는 하루</div>':''}<div class="add-day">${ibtn('new-block-on-day','plus','일정 추가',d)}</div></section>`;}).join('')}</div>`;}
function monthView(ctx){return `<section class="month"><div class="month-head">${'월화수목금토일'.split('').map(d=>`<span>${d}</span>`).join('')}</div><div class="month-grid">${monthDays(ctx.date).map(d=>{const blocks=dayBlocks(ctx.state,d);const due=leaves(ctx.state.items).filter(i=>i.deadline===d&&i.status!=='done');const total=blocks.length+due.length;return `<div class="month-cell ${d.slice(0,7)!==ctx.date.slice(0,7)?'outside':''} ${d===dateKey()?'today':''}" data-drop-day="${d}"><button class="month-date" data-action="select-day" data-id="${d}" aria-label="${d} 일정">${+d.slice(8)}</button>${blocks.slice(0,2).map(b=>`<button class="month-entry" data-action="edit-block" data-id="${b.id}" title="${esc(b.title)}">${b.allDay?'':timeKey(b.start)+' '}${esc(b.title)}</button>`).join('')}${due.slice(0,Math.max(0,3-Math.min(blocks.length,2))).map(i=>`<button class="month-entry due" data-action="edit-item" data-id="${i.id}" title="${esc(i.title)}">마감 · ${esc(i.title)}</button>`).join('')}${total>3?`<button class="tiny muted" data-action="select-day" data-id="${d}">+${total-3} 더 보기</button>`:''}</div>`;}).join('')}</div></section>`;}
function boardView(ctx){const items=leaves(ctx.state.items);return `<div class="filters">${btn('add-item','할 일 추가',{ico:'plus'})}${btn('chat-breakdown','큰 업무 쪼개기',{cls:'ghost',ico:'link'})}<span class="tiny muted">모바일에서는 카드를 눌러 상태를 변경하세요.</span></div><div class="kanban">${[['todo','할 일'],['doing','진행 중'],['done','완료']].map(([status,label])=>{const list=items.filter(i=>i.status===status).sort((a,b)=>priority(b)-priority(a));return `<section class="kanban-column" data-drop-status="${status}"><h2>${label}<span class="muted">${list.length}</span></h2>${list.map(i=>`<article class="kanban-card" draggable="true" data-drag-type="item" data-id="${i.id}"><div class="row">${check(i)}<button class="title-button" data-action="edit-item" data-id="${i.id}">${esc(i.title)}</button></div><div class="meta"><span>${esc(i.category)}</span><span>${duration(i.estimatedMinutes)}</span>${i.deadline?`<span>${deadlineText(i.deadline)}</span>`:''}</div>${status==='doing'?progress(i.progress):''}</article>`).join('')||'<p class="empty-plain">업무가 없습니다.</p>'}</section>`;}).join('')}</div>`;}
function projectsView(ctx){const projects=ctx.state.items.filter(i=>i.kind==='project');return projects.map(p=>{const children=ctx.state.items.filter(i=>i.parentId===p.id);return `<details class="panel project-panel" open><summary>${icon('folder')}<div class="grow"><strong>${esc(p.title)}</strong><span>${deadlineText(p.deadline)} · ${children.filter(i=>i.status==='done').length}/${children.length}개 완료</span></div><span class="small muted">${p.progress}%</span></summary><div class="project-tasks">${progress(p.progress)}<div class="space-top">${children.map(t=>`${taskRow(t,ctx.state.items)}<div class="indent">${ctx.state.items.filter(i=>i.parentId===t.id).map(i=>taskRow(i,ctx.state.items)).join('')}${btn('add-subtask','세부 업무',{id:t.id,cls:'ghost',ico:'plus'})}</div>`).join('')||'<p class="empty-plain">이 프로젝트의 첫 업무를 추가하세요.</p>'}</div><div class="actions space-top">${btn('add-task-to-project','업무 추가',{id:p.id,ico:'plus'})}${btn('edit-item','프로젝트 수정',{id:p.id,cls:'ghost'})}</div></div></details>`;}).join('')||empty('큰 목표를 프로젝트로 묶으세요.','프로젝트 → 업무 → 세부 업무, 3단계로 정리합니다.','add-project','프로젝트 만들기');}
function settingsView(ctx){const s=ctx.state.settings;const pending=ctx.snapshot.proposals.filter(p=>p.status==='pending');return `<div class="settings-grid"><section class="panel settings-card"><h2>ChatGPT와 함께 계획하기</h2><p>연결된 Supabase 도구를 통해 이 보드를 읽고 수정합니다. 변경안은 먼저 제안하고, 승인 후 반영합니다. 모든 일반 대화를 자동 수집하지는 않습니다.</p><div class="actions">${btn('chat-today','오늘 일정 불러오기',{ico:'link'})}${btn('chat-week','이번 주 계획 짜기')}${btn('chat-breakdown','큰 업무 쪼개기')}</div><div class="split-line"></div><h3>승인 대기 <span class="muted">${pending.length}</span></h3>${pending.map(p=>`<div class="task-row"><button class="title-button grow" data-action="proposal" data-id="${p.id}">${esc(p.title)}</button>${btn('proposal','검토',{id:p.id})}</div>`).join('')||'<p class="empty-plain">검토할 제안이 없습니다.</p>'}${btn('import-proposal','제안 JSON 가져오기',{cls:'ghost'})}<p class="hint">별도 OpenAI API 키나 유료 모델 호출을 사용하지 않습니다.</p></section><section class="panel settings-card"><h2>Google Calendar</h2><p>Google 캘린더를 직접 조회·수정하려면 본인 계정의 OAuth 웹 클라이언트 등록과 동의가 필요합니다. 등록 전에는 ChatGPT의 Google Calendar 도구로 가져오기를 요청할 수 있습니다.</p><form id="google-config-form" class="stack"><div><label for="google-client-id">OAuth 웹 클라이언트 ID</label><input id="google-client-id" name="googleClientId" value="${esc(s.googleClientId||'')}" placeholder="…apps.googleusercontent.com" autocomplete="off"></div><div><label for="google-calendar-id">연결할 캘린더 ID</label><input id="google-calendar-id" name="googleCalendarId" value="${esc(s.googleCalendarId||'primary')}" placeholder="primary"></div><div class="actions"><button class="button" type="submit">설정 저장</button>${btn('google-connect','Google 연결',{ico:'link'})}</div></form><div class="actions space-top">${btn('google-sync','앞뒤 한 달 동기화',{ico:'refresh'})}${btn('google-help','설정 안내',{cls:'ghost'})}</div><p class="hint">${ctx.googleConnected?'현재 기기에서 Google 인증됨. 새로고침·토큰 만료 후 재인증이 필요합니다.':'현재 기기 Google 직접 인증: 연결 전'}</p></section><section class="panel settings-card"><h2>일하는 시간</h2><p>자동 배치의 기준입니다. 이미 잡힌 일정과 휴식시간은 피해서 배치합니다.</p><form id="preferences-form"><div class="fields">${[['workStart','평일 시작'],['workEnd','평일 종료'],['weekendStart','주말 시작'],['weekendEnd','주말 종료'],['lunchStart','휴식 시작'],['lunchEnd','휴식 종료']].map(([id,label])=>`<div><label for="pref-${id}">${label}</label><input id="pref-${id}" name="${id}" type="time" value="${esc(s[id])}" required></div>`).join('')}<div><label for="bufferMinutes">일정 사이 여유 · 분</label><input id="bufferMinutes" name="bufferMinutes" type="number" min="0" max="60" value="${Number(s.bufferMinutes)||0}" required></div><div><label for="weeklyGoal">주간 완료 목표 · 개</label><input id="weeklyGoal" name="weeklyGoal" type="number" min="1" max="100" value="${Number(s.weeklyGoal)||5}" required></div></div><div class="actions space-top"><button class="button primary" type="submit">저장</button><span class="tiny muted">시간대: Asia/Seoul</span></div></form></section><section class="panel settings-card"><h2>데이터와 보안</h2><p>데이터는 개인 저장공간에 보관됩니다. 연결키를 아는 사람은 보드에 접근할 수 있으므로 비밀번호처럼 관리하세요.</p><div class="actions">${btn('export','백업 내보내기',{ico:'download'})}${btn('import-backup','백업 병합',{ico:'upload'})}</div><div class="actions space-top">${btn('show-key','연결키 확인')}${btn('rotate-key','연결키 재발급',{cls:'ghost'})}${btn('disconnect','이 기기 연결 해제',{cls:'ghost',ico:'logout'})}</div><div class="split-line"></div><h3>최근 변경</h3>${ctx.snapshot.history.slice(0,6).map(h=>`<div class="history-row"><span>${h.source==='chatgpt'?'ChatGPT 승인':esc(h.source)} · ${h.count}건</span><span class="muted">${dateKey(h.createdAt).slice(5)} ${timeKey(h.createdAt)}</span></div>`).join('')||'<p class="empty-plain">변경 이력이 없습니다.</p>'}<p class="hint">서비스 요금제: 무료 범위 사용. 유료 업그레이드·결제 등록은 앱에서 수행하지 않습니다. 무료 한도·휴면 제한은 적용될 수 있습니다.</p></section></div>`;}
export function shell(ctx){const {view,snapshot,state,store}=ctx;const g=growth(state.items,state.settings);const pending=snapshot.proposals.filter(p=>p.status==='pending'&&+new Date(p.expires_at)>Date.now());let main=view==='today'?todayView(ctx):view==='week'?weekView(ctx):view==='month'?monthView(ctx):view==='board'?boardView(ctx):view==='projects'?projectsView(ctx):settingsView(ctx);return `<aside class="sidebar"><div class="brand"><span class="brandmark">D</span>Dayboard</div><div><div class="nav-label">MY WORKSPACE</div><nav class="nav" aria-label="주 메뉴">${nav(view)}</nav></div><div class="sidebar-bottom"><button class="xp-mini" data-action="growth"><div class="xp-label"><strong>Lv. ${g.level}</strong><span class="muted">${g.levelXP} / 250 XP</span></div>${progress(g.levelXP/250*100)}<div class="tiny muted" style="margin-top:9px">이번 주 ${g.weekly} / ${g.goal}개 완료</div></button><div class="tiny muted" style="padding-left:13px">하나씩, 꾸준하게.</div></div></aside><div class="shell"><header class="topbar"><div class="topbar-left"><span class="eyeline">나의 보드</span><span class="sync"><span class="dot ${store.demo?'demo':store.error?'error':''}"></span>${store.demo?'샘플 데이터':store.busy?'저장 중':store.error?'연결 확인 필요':'저장됨'}</span></div><div class="topbar-right"><span class="search-top">${ibtn('search','search','업무 검색 · Ctrl K')}</span>${ibtn('refresh','refresh','최신 일정 불러오기')}${ibtn('growth','trophy','성장 기록')}${btn('add-item','할 일 추가',{cls:'primary',ico:'plus'})}</div></header>${!navigator.onLine||store.error?`<div class="offline-banner">${!navigator.onLine?'오프라인입니다. 네트워크 연결 후 다시 저장하세요.':'동기화 확인이 필요합니다. 새로고침 버튼을 눌러주세요.'}</div>`:''}<div class="content">${store.demo?`<div class="notice warning"><span>샘플 보드입니다. 변경 내용은 클라우드에 저장되지 않습니다.</span>${btn('exit-demo','내 보드 연결')}</div>`:''}${heading(ctx)}${pending.length&&view!=='settings'?`<div class="notice"><span>검토할 일정 제안이 ${pending.length}개 있습니다.</span>${btn('proposal','검토하기',{id:pending[0].id})}</div>`:''}${main}</div></div><nav class="mobile-nav" aria-label="모바일 메뉴">${nav(view,true)}</nav>`;}
export function modalFrame(title,body,footer='',form=''){return `${form?`<form id="${form}">`:''}<div class="modal-head"><h2 id="modal-title">${title}</h2>${ibtn('close','close','닫기')}</div><div class="modal-body">${body}<div id="modal-error" class="error-text" role="alert"></div></div>${footer?`<div class="modal-foot">${footer}</div>`:''}${form?'</form>':''}`;}
