import { localeCodes } from '../data/locales.mjs'; import { tests } from '../data/catalog.mjs';
export async function GET({site}){ const origin=site||new URL('https://501-studio.github.io'); const base=import.meta.env.BASE_URL; const urls=[]; for(const l of localeCodes){ urls.push(`${base}${l}/`,`${base}${l}/privacy/`,`${base}${l}/terms/`); for(const t of tests) urls.push(`${base}${l}/tests/${t.slug}/`); } const esc=s=>s.replace(/&/g,'&amp;'); const body=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>${esc(new URL(u,origin).href)}</loc></url>`).join('')}</urlset>`; return new Response(body,{headers:{'Content-Type':'application/xml'}}); }